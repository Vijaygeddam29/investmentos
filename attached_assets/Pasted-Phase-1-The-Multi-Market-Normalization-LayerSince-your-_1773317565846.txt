Phase 1: The Multi-Market & Normalization LayerSince your warehouse is live, we need to ensure the data flowing into it is "Clean" for global markets.Updated Prompt for Replit:"I have the Factor Warehouse table ready. Now, I need to standardize the input data for Global Markets:Currency Normalization: Update the financial_metrics fetcher to store the original currency (GBP, INR, etc.) but always save an fx_normalized_usd value.Scoring Constraint: Modify the scoring engine to only perform math on the USD-normalized values to ensure a 'Level Playing Field' across markets.Universe Check: Populate the companies table with 50 diverse tickers (NYSE, NASDAQ, LSE, and Asian markets) to test the multi-currency logic.API Filters: Update GET /companies to allow filtering by sector, country, and market_cap directly from the Factor Warehouse."Phase 2: The "Smart Worker" & Signal DetectionThis is the "Trick" in action. Since the table exists, we now need the engine to fill it intelligently without crashing.Updated Prompt for Replit:"I need to implement the Asynchronous Worker and Incremental Update logic:The Worker: Instead of running the pipeline on the main thread, create a background worker that processes tickers in batches of 50.Incremental Logic: Only trigger a full Fortress/Rocket rescore if the last_run_date is older than 30 days or the next_earnings_date has passed. Wave/Entry scores should still update daily.Score Deltas: Create a logic to compare today’s snapshot with the snapshot from 7 days ago. Store this as weekly_delta.Top Movers: Create an endpoint GET /signals/movers that returns the top 10 stocks with the highest weekly_delta in Fortress and Rocket scores."Phase 3: The Research Terminal (UI/UX)This turns your raw data into the 'Professional' dashboard you're aiming for.Updated Prompt for Replit:"Let's build out the Frontend Research tools using Recharts:Interactive Charts: On the company detail page, plot 3-year Price History against the Fortress Score and Rocket Score from the warehouse.Valuation Band Chart: Add a 'Price vs. Intrinsic Value' graph. Shade the area where price is $< 75\%$ of DCF as the 'BUY' zone.The Global Screener: Build a Screener page that allows me to filter by any factor in the warehouse (e.g., 'Show me UK stocks with Fortress Score > 0.8').Signal Widget: Add a 'Momentum/Quality Improving' section to the home dashboard using the delta logic we built in Phase 2.Hover Effects: Ensure all navigation tabs/icons turn dark when hovered over for better visual feedback."